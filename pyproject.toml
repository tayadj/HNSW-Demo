[build-system]
requires = [
    "scikit-build-core==1.0", 
    "nanobind==3.0.1",
    "cmake==4.4",
    "ninja==1.13"
]
build-backend = "scikit_build_core.build"

[project]
name = "hnsw"
description = "HNSW C++ Implementation."
version = "0.0.1"